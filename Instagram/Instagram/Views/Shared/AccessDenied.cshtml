@{
    ViewData["Title"] = "Доступ запрещен";
}

<div class="d-flex justify-content-center align-items-center vh-100 bg-light">
    <div class="text-center shadow p-5 rounded bg-white" style="max-width: 500px;">
        <h1 class="display-4 text-danger mb-4"><i class="bi bi-shield-lock-fill"></i> Доступ запрещен</h1>
        <p class="lead text-muted mb-4">Извините, у вас недостаточно прав для доступа к этой странице.</p>
        <a href="@Url.Action("Profile", "Publication")" class="btn btn-primary btn-lg">Вернуться в профиль</a>
    </div>
</div>
